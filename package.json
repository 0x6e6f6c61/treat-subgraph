{
  "name": "treat",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ treat",
    "create-local": "graph create --node http://localhost:8020/ treat",
    "remove-local": "graph remove --node http://localhost:8020/ treat",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 treat"
  },
  "dependencies": {
    "@amxx/graphprotocol-utils": "^1.1.0-alpha.0",
    "@graphprotocol/graph-ts": "^0.24.1",
    "web3": "^1.6.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.22.4"
  }
}
